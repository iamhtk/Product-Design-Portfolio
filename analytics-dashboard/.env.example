# Google Analytics 4 Configuration
# Get your Property ID from: Admin > Property Settings in Google Analytics
VITE_GA4_PROPERTY_ID=your-property-id-here

# Service Account Key (base64 encoded JSON)
# Generate from: Google Cloud Console > IAM & Admin > Service Accounts
# Download JSON key, then base64 encode it: cat key.json | base64
VITE_GA4_SERVICE_ACCOUNT_KEY=your-base64-encoded-key-here

# Note: For production, use a backend API to securely handle credentials
# The frontend should call your API endpoint, not GA4 directly
