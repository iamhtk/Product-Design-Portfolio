/**
 * Set to false to hide the overview paragraph on all project pages.
 * Per-project: use `const overview: string | undefined = undefined` to hide on that project only.
 */
export const SHOW_PROJECT_OVERVIEW = true;
